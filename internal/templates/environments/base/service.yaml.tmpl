apiVersion: v1
kind: Service
metadata:
  name: {{.Name}}
  labels:
    app.kubernetes.io/name: {{.Name}}
spec:
  type: ClusterIP
  ports:
    - port: {{.Port}}
      targetPort: {{.Port}}
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{.Name}}
